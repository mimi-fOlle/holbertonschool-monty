#include "monty.h"

/**
 * nop - Opcode that doesn't do anything
 * @stack: A stack
 * @line_nb: Monty file's line number
 */
void nop(
		__attribute__((unused)) stack_t **stack,
		__attribute__((unused)) unsigned int line_nb
){}
